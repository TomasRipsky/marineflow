# =============================================================================
# MarineFlow — ML Training Dependencies
# ml/training/requirements.txt
#
# Install: pip install -r ml/training/requirements.txt
#
# GCP auth uses Application Default Credentials (ADC).
# No service account key file required.
# Run: gcloud auth application-default login
# =============================================================================

# --- GCP ---
google-cloud-bigquery==3.13.0   # pinned alongside dbt-bigquery to avoid _CELLDATA_FROM_JSON error
google-cloud-storage==2.14.0

# --- Data ---
pandas==2.2.1
numpy==1.26.4
pyarrow==15.0.2                 # required by BigQuery → pandas conversion

# --- ML ---
scikit-learn==1.4.1
xgboost==2.0.3
joblib==1.3.2                   # model serialization

# --- Utils ---
db-dtypes==1.2.0                # BigQuery date/timestamp types in pandas